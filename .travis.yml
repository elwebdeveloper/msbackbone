language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-msbackbone' ]; then cd .. && eval "mv $currentfolder generator-msbackbone" && cd 'generator-msbackbone'; fi

